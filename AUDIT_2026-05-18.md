# AUDIT_2026-05-18 — Index of audit findings

> This file is the canonical index for the audit IDs (`K-*`, `S-*`)
> that are referenced throughout the codebase. Full remediation
> commentary, before/after diffs and verification notes live in
> [REMEDIATION_PLAN.md](REMEDIATION_PLAN.md). New audit findings and
> their fixes (audit T1.* / T2.* / T4.*, 2026-05-25) are appended at
> the end.
>
> The file was reconstructed on 2026-05-25 because the original
> document — referenced from 27 files but never committed to the repo
> — was lost during an earlier pass. Operators who land on a `K-`
> or `S-` citation should look here first; deep dives belong in the
> remediation plan.

## K-* — Critical / structural findings

| ID    | Topic                                              | Status                    |
|-------|----------------------------------------------------|---------------------------|
| K-1   | TLS cipher order (NSS internal, not launcher-controlled) | Partial — see REMEDIATION_PLAN §2.3 |
| K-2   | HTTP/2 SETTINGS frame not spoofed                  | Experimental patch only   |
| K-3   | TLS / NSS launcher integration                     | Fixed                     |
| K-5   | Cloud broker / Remote Debugger exposure            | Fixed (force-local=true)  |
| K-6   | navigator.webdriver — Juggler-only gate            | Fixed (WebIDL gate)       |
| K-7   | nsGlobalWindowInner cross-manifest overlap         | Tracked, expected         |
| K-8   | uTLS sidecar (experimental, FF150 maps to FF120)   | Off the production path   |
| K-10  | MaskConfig stderr leak (`CAMOU_MASKCFG_DEBUG`)     | Fixed                     |
| K-11  | CapSolver licensing / third-party egress           | See NOTICE.md             |
| K-12  | Identity blob readable from `/proc/<pid>/environ`  | Fixed (T2.1, file default)|
| K-13  | Disabled async tests                               | Tracked                   |
| K-14  | Build artefacts ignored                            | Tracked in .gitignore     |
| K-15  | Env-var chunking size limit                        | Fixed                     |
| K-16  | `camoufox.cfg` security defaults                   | Fixed (§1.2)              |
| K-17  | Dockerfile hardening                               | Fixed                     |
| K-18  | Secure-config file transport (`CAMOU_CONFIG_FILE`) | Fixed; now default (T2.1) |
| K-19  | CAPTCHA scanner experimental status                | Documented                |
| K-20  | Dockerfile.slim minimum surface                    | Fixed                     |
| K-21  | HTTP/2 fingerprint patch (`http2-fingerprint.patch`)| Experimental             |

## S-* — Secondary findings

| ID    | Topic                                              |
|-------|----------------------------------------------------|
| S-B   | Alternative transport for identity blob (file)     |
| S-D   | Device-profile sampling determinism                |
| S-E   | Font list / preference parity                      |
| S-F   | CAPTCHA flow telemetry                             |
| S-G   | GREASE / TLS extension ordering                    |
| S-P   | Patch-conflict allowlist                           |
| S-R   | TLS resumption / session caching                   |
| S-S   | TLS profile selection (NSS source verification)    |
| S-T   | Test infrastructure                                |

## T-* — 2026-05-25 expert-audit follow-up

These are the audit items addressed in the 2026-05-25 remediation
pass (responding to two independent expert critiques). Concrete
diffs and rationale live in [REMEDIATION_PLAN.md](REMEDIATION_PLAN.md)
where applicable.

| ID    | Topic                                                    | Status |
|-------|----------------------------------------------------------|--------|
| T1.1  | Randomized CAPTCHA scanner markers (`window.__csi` etc.)| Fixed  |
| T1.2  | `IdentityCoherenceError` enforced unless explicitly bypassed | Fixed  |
| T1.3  | `headers.User-Agent` / `headers.Accept-Language` cross-check | Fixed  |
| T1.4  | Shared `CamouEnv::IsTruthyEnv` (MaskConfig ↔ CamouTLSOverride) | Fixed  |
| T1.5  | Randomized MutationObserver debounce (600–1000 ms)      | Fixed  |
| T2.1  | `CAMOU_CONFIG_FILE` is the launcher default              | Fixed  |
| T2.2  | External CAPTCHA solvers gated by `CAMOU_CAPTCHA_ALLOW_EXTERNAL` | Fixed  |
| T2.3  | CapSolver fails fast on missing API key                  | Fixed  |
| T2.4  | K8s session snapshots redact proxy / secret fields       | Fixed  |
| T4.1  | Audit index reconstructed (this file)                    | Fixed  |
| T4.3  | `canvas:noiseSeed` exposed in `properties.json` schema   | Fixed  |
| T3.1  | HTTP/2 fingerprint emission default ON (consumer in network.yaml) | Fixed  |
| T3.2  | `firefoxBaselineIdentity()` replaces HelloFirefox_120 fallback for FF135+ | Fixed  |
| T3.3  | `upstream.sh` bumped to 150.0.3; weekly MFSA drift CI job | Fixed  |
| T3.4  | Fingerprint parity harness (probes.html + harness.py + baseline) | Fixed (placeholder baseline) |

## Structural items still open

These items require a Firefox source rebase, a working FF150 binary
on the same host as Camoufox, or sustained upstream contribution and
are intentionally not in scope for the 2026-05-25 pass:

- **Real Firefox 150 baseline capture** — `tests/fingerprint_parity/
  baseline_stock_firefox_150.json` ships as a schema-shaped placeholder.
  Regenerate against a real stock FF150 install (see harness README)
  before relying on the parity check for go/no-go decisions.
- **CI wiring of the parity harness** — needs the build matrix to
  produce a runnable `camoufox-bin` artefact in the same workflow.
  Skeleton job is documented in
  `tests/fingerprint_parity/README.md`.
- **utls upstream contribution** — `firefoxBaselineIdentity()` is a
  static FF135-derived spec, not a verified FF150 ClientHello capture.
  Once the parity harness has real FF150 captures, contribute a
  `HelloFirefox_150` preset back to refraction-networking/utls so the
  static spec can go away.
- **Mozilla beta-channel drift** — the audited
  `http2-fingerprint.patch` anchors were validated against
  mozilla-beta tip on 2026-05-18. Subsequent FF point releases may
  shift the four target lines; the `apply-check` job is the canary.
